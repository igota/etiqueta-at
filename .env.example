# Copie este arquivo para ".env" (ou defina as variáveis no ambiente/serviço
# Windows) e preencha com os valores reais do seu ambiente. Nunca comite o
# ".env" real — ele já está no .gitignore.

# URL base do sistema do hospital (sem a barra final)
HOSPITAL_BASE_URL=http://10.2.2.8:8080/pacientehrn

# Chave de sessão do Flask
FLASK_SECRET_KEY=troque-por-uma-chave-aleatoria-longa
